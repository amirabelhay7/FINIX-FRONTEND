import { NgModule } from '@angular/core';
import { CommonModule } from '@angular/common';
import { RouterModule } from '@angular/router';

// For now, this is a placeholder for agent wallet functionality
// We can implement the full agent wallet later

@NgModule({
  imports: [
    CommonModule,
    RouterModule
  ]
})
export class WalletModule { }
